# Django Trading Card Vault - Environment Configuration
# Copy this file to .env and customize the values

# Django Settings
DEBUG=1
SECRET_KEY=your-secret-key-here-change-this-in-production
DJANGO_LOG_LEVEL=INFO

# Database Configuration
# For development (SQLite) - leave DATABASE_URL empty
# For production (PostgreSQL) - set DATABASE_URL
DATABASE_URL=postgresql://postgres:postgres@db:5432/trading_cards

# Security Settings
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Production Settings (set DEBUG=0 for production)
# SECRET_KEY=your-very-secure-secret-key-here
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# DATABASE_URL=postgresql://user:password@db:5432/trading_cards_prod

# Optional: Custom Database Settings
# POSTGRES_DB=trading_cards
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres

# Optional: Email Configuration (for production)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password 